/*
** EPITECH PROJECT, 2021
** Indie
** File description:
** PlayerTests
*/

#include <gtest/gtest.h>
#include "../Tests/Lib/XRaylib/Raylib/Image/Image.hpp"

/*
TEST(ImageTest, unloadImageTest)
{
    try {
        Raylib::Image test;
        test.unloadImage();
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid illboard texture defined by source", ex.what());
    }
}


TEST(ImageTest, exportImageTest)
{
    Raylib::Image test;
    Raylib::Image test1;
    const char *fileName = "a";
    EXPECT_EQ(false, test.exportImage(test1,fileName));
}



TEST(ImageTest, getPixelDataSizeTest)
{
    try {
        Raylib::Image test;
        test.getPixelDataSize();
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid illboard texture defined by source", ex.what());
    }
}


TEST(ImageTest, getTextureDataTest)
{
    try {
        Raylib::Image test;
        //Raylib::Color gray;
        Image image = LoadImage("../../resources/assets/bg.png");
        ::Texture texture = LoadTextureFromImage(image);

        test.getTextureData(texture);
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid illboard texture defined by source", ex.what());
    }
}

*/