/*
** EPITECH PROJECT, 2021
** Indie
** File description:
** PlayerTests
*/

#include <gtest/gtest.h>
#include "../Tests/Lib/XRaylib/Raylib/File/File.hpp"
/*

TEST(FileTest, loadFileDataTest)
{
    try {
        Raylib::File test = fopen("test.txt", "r");
        unsigned int i = 0;
        test.loadFileData(test,i);
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid Load file data", ex.what());
    }
}


TEST(FileTest, unloadFileDataTest)
{
    try {
        Raylib::File test;
        auto a = "a";
        unsigned int i = 0;
        test.loadFileData(a,i);
        test.unloadFileData(a);
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid Load file data", ex.what());
    }
}

*/