
#include <gtest/gtest.h>
#include "../Lib/XRaylib/XRay/includes/Input.hpp"

/*
TEST(InputTest, pass)
{
    try {
        Input test;

        auto res = test.Input()
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid input", ex.what());
    }
}



TEST(InputTest, updateGamepadPass)
{
    try {
        Input test;
        test.updateGamepad();
    } catch (std::exception &ex) {
        EXPECT_STREQ("ERROR: Invalid input for updateGamepad", ex.what());
    }
}

*/